DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
JWT_SECRET=changeme-replace-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# ── Ports (change both together if you move either server) ────
BACKEND_PORT=8000
FRONTEND_PORT=5173
