# URL of the FastAPI backend — consumed by api/auth.ts and api/org.ts
VITE_API_URL=http://localhost:8000

# Port this Vite dev server will listen on (must match FRONTEND_PORT in backend/.env)
VITE_PORT=5173

# Backend port — used by vite.config.ts to build the proxy target
VITE_BACKEND_PORT=8000
